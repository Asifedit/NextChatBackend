const { redis } = require("../Middleware/redis")


// this it reatelimitator 
const RateLimiter = async () => {
    // make a bucket and incrize valure every time call with key,maximum number allow etc and retun value true or false
    
}